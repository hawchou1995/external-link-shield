external_links_in_new_tab:
  default: true
  description: "Always open external links in a new tab."

enable_exit_confirmation:
  default: true
  description: "Show a confirmation modal when a user clicks on an external link (for Normal, Risky, and Dangerous domains)."

internal_domains:
  type: list
  default: ""
  description: "Internal Domains: No icon, no confirmation modal. (e.g. your CDN or related sites)"

excluded_domains:
  type: list
  default: "youtube.com|github.com|wikipedia.org"
  description: "Trusted Domains: Shows a Lock 🔒 icon, skips confirmation modal."

risky_domains:
  type: list
  default: "bit.ly|tinyurl.com|t.co"
  description: "Risky Domains: Shows a Warning ⚠️ icon, opens a warning modal asking for confirmation."

dangerous_domains:
  type: list
  default: "known-phishing-site.com"
  description: "Dangerous Domains: Shows a Skull ☠️ icon. Modal only allows copying the URL, direct access is disabled."

blocked_domains:
  type: list
  default: "spam-site.com"
  description: "Blocked Domains: Links are replaced with '[Blocked Link]' text and cannot be clicked."
