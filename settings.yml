# --- 核心功能开关 ---
external_links_in_new_tab:
  default: true
  description: "Always open external links in a new tab."

enable_exit_confirmation:
  default: true
  description: "Show a confirmation modal when a user clicks on an external link (for Normal, Risky, and Dangerous domains)."

# --- 权限控制 (已恢复) ---
enable_anonymous_blocking:
  type: bool
  default: true
  description: "Replace external links for anonymous users with a login prompt."

anonymous_redirect_url:
  type: string
  default: "/login"
  description: "URL where anonymous users are redirected when trying to access external links."

enable_tl0_blocking:
  type: bool
  default: true
  description: "Replace external links for TL0 users with a trust-level information link."

tl0_redirect_url:
  type: string
  default: ""
  description: "URL where TL0 users are redirected when click the link."

enable_tl1_manual_reveal:
  type: bool
  default: true
  description: "Require TL1 users to manually reveal external links (Click to view)."

# --- 6级域名分类配置 ---
internal_domains:
  type: list
  default: ""
  description: "Internal Domains: No icon, no confirmation modal."

excluded_domains:
  type: list
  default: "youtube.com|github.com|wikipedia.org"
  description: "Trusted Domains: Shows a Lock 🔒 icon, skips confirmation modal AND skips User Trust Level checks."

risky_domains:
  type: list
  default: "bit.ly|tinyurl.com|t.co"
  description: "Risky Domains: Shows a Warning ⚠️ icon, opens a warning modal."

dangerous_domains:
  type: list
  default: "known-phishing-site.com"
  description: "Dangerous Domains: Shows a Skull ☠️ icon. Copy only."

blocked_domains:
  type: list
  default: "spam-site.com"
  description: "Blocked Domains: Links are replaced with text."
